# FluidFramework Library For .NET
The FluidFramework library provides connectivity for client applications developed in .NET to Microsoft SQL Server using ADO.NET.

This package also serves as the core for related libraries that enable connectivity to other types of databases.